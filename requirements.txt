# NERVE — runtime and development dependencies
# Python 3.11+

# Web framework + ASGI server
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# Database (async MongoDB driver)
motor>=3.4,<4.0

# Schema validation + settings
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# Agent reasoning (Gemini via Vertex AI)
google-cloud-aiplatform>=1.50,<2.0

# Structured logging
structlog>=24.1,<25.0

# Retry policy
tenacity>=8.2,<9.0

# MCP client SDK
mcp>=1.0,<2.0

# GitLab REST API calls (GitLab has no drop-in MCP server for these ops)
httpx>=0.27,<1.0

# Env file loading
python-dotenv>=1.0,<2.0

# Telegram bot (mobile approvals + notifications) — async, v20+
python-telegram-bot>=20.0,<22.0

# Auth — password hashing and JWT session tokens
passlib[bcrypt]>=1.7,<2.0
pyjwt>=2.8,<3.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
mongomock-motor>=0.0.29  # in-memory MongoDB for unit tests
