"""Agent system for NERVE.

All agents inherit from :class:`~agents.base_agent.BaseAgent` and coordinate
only through the orchestrator and the state layer — never directly with each
other (see CLAUDE.md "Agent System").
"""
